# Kakeibo

作業を始める前に、必ず `HANDOFF.md` を読んでから対応してください。アプリの背景・画面構成・データ構造・データ保存の仕組み（localStorage限定で複数端末同期はできない点）・認証の仕組みと既知の注意点がまとまっています。

機能や仕様についての質問には、`HANDOFF.md`に加えて実際のソースコード（`src/`以下の各ファイル）の中身も確認した上で答えてください。

大きな変更（データ構造の変更、保存方式の変更、認証まわりの変更など）を行った場合は、`HANDOFF.md`も合わせて更新してください。
